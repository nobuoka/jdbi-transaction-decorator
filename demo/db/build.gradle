dependencies {
    compile project(':demo:core')

    compile "info.vividcode.jdbi:transaction-decorator-jdbi:${demo_transaction_decorator_version}"

    compile "io.dropwizard:dropwizard-jdbi:${dropwizard_version}"

    testCompile "org.junit.jupiter:junit-jupiter-api:${rootProject.ext.junitVersion}"
    testRuntime "org.junit.jupiter:junit-jupiter-engine:${rootProject.ext.junitVersion}"
    testCompile 'org.hamcrest:java-hamcrest:2.0.0.0'
    testCompile 'com.h2database:h2:1.3.176'
}
